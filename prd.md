# 1. 产品概述

## 产品名称
私人医生助手 (Personal Doctor Assistant)

## 定位一句话
基于大语言模型微调的AI私人医生，通过医学报告图片一键生成异常指标分析与个性化健康建议。

## 目标用户
| 用户群体 | 特征 | 痛点 |
|----------|------|------|
| 体检用户（核心） | 每年体检1-2次，收到纸质/电子报告后看不懂指标含义 | 医学术语晦涩，不知道哪些指标需要关注 |
| 慢性病患者 | 需定期复查监测指标（血糖/血脂/尿酸等） | 无法快速判断指标变化趋势和健康风险 |
| 中老年人群 | 常有多项指标异常，就医前想初步了解 | 挂号和咨询成本高，行动不便 |
| 健康管理爱好者 | 主动监测各项身体指标，做健康管理 | 缺少一个集中的解读和分析工具 |

## 核心痛点
1. **医学术语壁垒**：普通人看不懂"谷丙转氨酶""低密度脂蛋白胆固醇"等专业术语的含义
2. **异常指标遗漏**：体检报告指标多（20-50项），非专业人士容易忽略轻微异常
3. **缺乏 actionable 建议**：即使知道指标异常，也不知道该做什么（饮食/运动/就医方向）
4. **时间成本高**：为一个小问题跑医院排号咨询效率低
5. **碎片化管理**：多次体检报告分散在不同医院/时间，缺乏统一管理和趋势追踪

## 价值主张
"上传体检报告，3秒读懂你的健康状况。"
- 一键上传 → 自动识别异常指标 → 生成个性化健康建议
- 基于医学QA数据集微调的专业AI模型，而非通用大模型的"幻觉式"解答
- 本地部署，隐私数据不出门

# 2. 市场调研

## 市场现状
| 维度 | 现状 |
|------|------|
| 市场规模 | 中国健康管理市场规模2025年预计突破3000亿元，其中AI健康助手赛道增速30%+ |
| 用户习惯 | 疫情后居民健康意识大幅提升，体检渗透率持续增长（年体检人次超6亿） |
| 技术成熟度 | LLM+多模态模型在2024-2025年快速成熟，Qwen2.5-VL等中文模型表现可比肩GPT-4V |
| 政策环境 | 国家推动"互联网+医疗健康"，AI辅助诊断获得政策支持但监管趋严 |

## 用户需求趋势
| 趋势 | 说明 |
|------|------|
| 从"看病"到"防病" | 用户不再等到生病才去医院，而是日常监测指标做预防 |
| 移动端优先 | 用户希望在手机上快速解读报告，不用打开电脑 |
| 即时反馈 | 等待时间超过30秒会导致用户流失 |
| 隐私敏感 | 医疗数据是最高级别的隐私数据，用户对数据上云有顾虑 |
| 简单直观 | 用户不需要复杂的功能列表，只需要"上传→看结果" |

## 机会点分析
| 机会点 | 分析 |
|--------|------|
| 中文医疗AI空白 | 国内外通用模型（GPT-4o/Gemini）在中文医学场景表现不佳，Qwen系列中文能力领先 |
| 本地部署需求 | 医院/诊所/企业健康中心需要数据不出私网的AI方案 |
| 体检产业链整合 | 可与体检机构（美年大健康/爱康国宾）合作，嵌入其报告解读流程 |
| 个性化健康管理 | 基于连续多份报告做趋势分析，提供真正个性化的健康管理方案 |

# 3. 竞品分析

## 竞品1：腾讯医典
| 维度 | 详情 |
|------|------|
| 产品形态 | 医疗科普+在线问诊App |
| 核心功能 | 症状自查、药品查询、专家科普、在线问诊 |
| 优点 | 内容专业（有医生团队审核），品牌信任度高，流量大 |
| 缺点 | 不支持医学报告图片识别；症状自查是决策树而非AI；需要手动输入症状 |
| 可借鉴点 | 科普内容的知识结构化方式；免责声明和医疗建议边界 |
| 差异化机会 | 专注"报告解读"这个具体场景，比腾讯医典更垂直、更自动化 |

## 竞品2：百度健康 / 阿里健康AI
| 维度 | 详情 |
|------|------|
| 产品形态 | 大厂生态内的健康AI工具（如百度灵医、阿里健康AI） |
| 核心功能 | 智能问诊、用药助手、在线挂号、健康资讯 |
| 优点 | 背靠大厂AI能力和用户流量；百度有文心大模型支撑 |
| 缺点 | 通用AI在医学垂直场景深度不足；数据上云隐私顾虑大；商业化包装过重 |
| 可借鉴点 | 大厂在AI健康领域的探索方向（多轮对话/药品识别） |
| 差异化机会 | 私有化部署能力；纯聚焦"医学报告→建议"这个窄场景做到极致 |

## 竞品3：ImageDiagnosis / 杏脉科技 (AI医学影像)
| 维度 | 详情 |
|------|------|
| 产品形态 | 面向医院的AI医学影像诊断系统 |
| 核心功能 | CT/X光/MRI/病理切片AI分析 |
| 优点 | 专业性强，通过NMPA认证，可辅助医生诊断 |
| 缺点 | 面向医院而非C端用户；需要PACS系统对接；使用门槛高 |
| 可借鉴点 | AI影像分析的pipeline设计（图像预处理→模型推理→结构化报告） |
| 差异化机会 | C端市场空白——不需要NMPA认证的"健康建议"类产品（非诊断） |

## 竞品4：ChatGPT / Claude (通用AI)
| 维度 | 详情 |
|------|------|
| 产品形态 | 通用对话AI |
| 核心功能 | 用户可以上传图片问"帮我分析这份体检报告" |
| 优点 | 对话能力强，能理解复杂提问；支持多轮追问 |
| 缺点 | 中文医学知识深度不足；给出建议不专业甚至危险；数据上传到海外服务器有隐私风险 |
| 可借鉴点 | 优秀的对话UI设计（流式输出、追问） |
| 差异化机会 | 领域垂直专业度远超通用AI；LoRA微调后可用专业医学术语准确回答；支持本地部署 |

## 竞争格局总结
```
                        通用性（广度）
                             ｜高
                     ChatGPT/Claude ●
                             ｜
        腾讯医典 ●———————+———————● 百度健康
                             ｜
                             ｜
        私人医生助手 ●———————————● 杏脉科技
                             ｜
                             ｜低
                        专业性（深度）
```

私人医生助手的定位：在"体检报告解读"这个特定场景做到比通用AI更专业、比腾讯医典/百度健康更自动化、比杏脉科技更面向C端。

# 4. MVP定义

## P0 核心功能
| 功能 | 优先级 | 预估工时 | 说明 |
|------|--------|----------|------|
| 医学报告图片上传 | P0 | 0.5周 | 支持JPG/PNG, 图片预览, 缩放到安全尺寸 |
| VL模型异常指标识别 | P0 | 2周 | Qwen2.5-VL-7B微调, 识别报告中的异常指标 |
| LLM健康建议生成 | P0 | 2周 | Qwen2.5-14B微调, 基于异常指标给出饮食/运动/生活方式建议 |
| 聊天式前端界面 | P0 | 1周 | 类聊天UI, 显示图片+分析结果+建议 |
| FastAPI后端服务 | P0 | 1周 | POST/image端点, 串联VL→LLM管道 |
| 模型训练pipeline | P0 | 2周 | 数据准备+LoRA微调+模型评测 |

## P1 延后功能
| 功能 | 预估工时 | 说明 |
|------|----------|------|
| 用户登录/注册 | 1周 | 邮箱或手机号注册 |
| 历史记录 | 1周 | 保存每次上传的报告和分析结果 |
| 多轮对话追问 | 1.5周 | 用户可针对结果继续提问 |
| 离线PDF报告 | 0.5周 | 将分析结果导出为PDF |
| 评测可视化看板 | 1周 | BLEU/ROUGE指标趋势图 |

## P2 远期功能
| 功能 | 说明 |
|------|------|
| 语音输入/播报 | 支持语音描述症状和语音朗读分析结果 |
| 多报告趋势分析 | 跨时间维度的指标变化图表 |
| 用药/复诊提醒 | 基于诊断结果的智能日程管理 |
| 可穿戴设备接入 | Apple Watch/华为手环数据整合 |
| 医生审核模式 | 真实医生可审核和修正AI建议 |
| 多语言 | 英文/日文等体检报告支持 |

## 明确不做什么（MVP）
- 不做在线问诊/挂号（医疗决策闭环，需要资质）
- 不做药品推荐（涉及处方药合规）
- 不做医院PACS系统对接（面向C端个人用户）
- 不做影像级诊断（如CT病灶检测，需要NMPA认证）
- 不做知识库/FAQ问答（偏离核心场景）
- 不做移动端原生App（PWA/响应式Web足够）

# 5. 功能详细设计

## 模块1：医学报告图片上传
| 维度 | 详情 |
|------|------|
| 功能描述 | 用户通过前端页面选择本地的医学报告图片，支持预览和上传 |
| 用户价值 | 零门槛上传，手机拍照即可 |
| 输入 | JPG/PNG格式图片文件，单张，建议最长边不超过1280px |
| 输出 | Base64编码的图片数据（后端），前端显示缩略图预览 |
| 关键逻辑 | 前端：FileReader读取→预览显示；后端：Pillow resize到最长边1280px→Base64编码 |
| 错误处理 | 文件过大(>20MB)→提示压缩；非图片格式→提示重新选择；未选图片→禁用发送按钮 |

## 模块2：异常指标自动识别（VL模型）
| 维度 | 详情 |
|------|------|
| 功能描述 | Qwen2.5-VL-7B微调模型接收医学报告图片，识别并列出所有异常指标 |
| 用户价值 | AI自动扫描报告，不会遗漏任何异常指标 |
| 输入 | Base64编码图片 + 提示词："请仔细分析这张医学检测报告图片，识别并列出其中的异常指标。如果没有发现异常指标，请明确说明'未发现异常指标'。" |
| 输出 | 结构化文本：异常指标列表（如 "血糖: 6.8mmol/L ↑（偏高），建议复查空腹血糖"）或 "未发现异常指标" |
| 关键逻辑 | vLLM部署VL模型于端口9002；FastAPI通过httpx调用其OpenAI兼容API |
| max_tokens | 256（异常指标摘要不需要太长） |
| 错误处理 | 模型响应格式异常→返回错误提示；超时→重试1次后报错 |

## 模块3：健康建议生成（LLM模型）
| 维度 | 详情 |
|------|------|
| 功能描述 | Qwen2.5-14B微调模型基于VL输出的异常指标摘要，生成个性化的饮食/运动/生活方式建议 |
| 用户价值 | 不仅告诉你"哪里有问题"，还告诉你"该怎么办" |
| 输入 | VL模型输出的异常指标摘要 + 提示词："根据以下医学检测报告分析结果，提供相应的健康建议和注意事项……" |
| 输出 | 结构化健康建议：分饮食建议、运动建议、生活方式建议、就医建议等 |
| 关键逻辑 | vLLM部署LLM模型于端口8001，加载LoRA adapter "mylora"；串行调用VL→LLM |
| max_tokens | 512（建议内容需要详细但不过度） |
| 错误处理 | 模型响应异常→返回兜底建议信息；VL输出为空→直接提示用户重新上传 |

## 模块4：前端聊天界面
| 维度 | 详情 |
|------|------|
| 功能描述 | 类微信聊天的界面，用户发送图片→显示用户消息（图片缩略图）→加载动画→显示服务器回复（分析结果+健康建议） |
| 用户价值 | 交互直观，符合日常聊天习惯，学习成本为零 |
| 输入 | 用户点击图片选择→点击发送按钮 |
| 输出 | 分析结果（带Markdown格式）和健康建议（带Markdown格式）分别渲染 |
| 关键逻辑 | XHR异步请求（非fetch以确保兼容性）；加载动画含三点跳动+计时器（反馈等待时间）；Markdown→HTML转换器（支持标题/粗体/列表） |
| 错误处理 | 网络错误→显示"网络错误，请检查连接"；服务器错误→显示具体错误码和信息；超时→显示"请求超时，请稍后重试" |

## 模块5：后端FastAPI服务
| 维度 | 详情 |
|------|------|
| 功能描述 | 接收前端请求，串联VL+LLM双模型调用，返回结构化JSON响应 |
| 用户价值 | 无感知的端到端处理 |
| 输入 | POST /image，Content-Type: multipart/form-data，files字段 |
| 输出 | JSON: { "analysis_result": "...", "health_recommendations": "..." } |
| 关键逻辑 | 1. 接收图片→resize→Base64编码 2. 调用VL vLLM(9002)→获取摘要 3. 调用LLM vLLM(8001)→获取建议 4. 返回JSON |
| 错误处理 | httpx.RequestError/HTTPStatusError→500+具体错误信息；KeyError→500+格式异常信息 |

# 6. 用户流程（从进入产品到完成核心目标的步骤）

```
[用户打开页面]
    │
    ▼
[看到欢迎语："欢迎使用医疗报告助手！请上传您的医疗检测报告图片以获取分析和健康建议。"]
    │
    ▼
[点击"+"按钮 / 点击"选择图片"区域]
    │
    ▼
[系统弹出文件选择器（支持相册/拍照/文件管理器）]
    │
    ▼
[选择图片 → 前端显示缩略图预览 + "发送"按钮变为可点击]
    │
    ▼ (可选)
[点击"×"移除图片，回到初始状态]  ←───┐
    │                                  │
    ▼                                  │
[点击"发送"按钮]                       │
    │                                  │
    ▼                                  │
[用户消息区域显示上传的图片]            │
    │                                  │
    ▼                                  │
[显示加载动画（三点跳动+计时器）]       │
    │                                  │
    ▼                                  │
[后端处理：图片缩放→VL识别→LLM建议]    │
    │                                  │
    ├── 成功 ──→ [显示"检测结果分析"区域 + "健康建议"区域] ──→ [满意?] ──→ [关闭/继续]
    │                                                                        │
    └── 失败 ──→ [显示错误消息] ───────────────────────────────────────────────┘
                           (用户修正后重新上传)
```

## 核心路径
1. 打开页面 → 2. 选择图片 → 3. 发送 → 4. 等待5-15秒 → 5. 查看分析结果和建议 → 6. 完成

## 边缘路径
- 用户上传非医学图片（如风景照）→ VL模型输出"未识别到医学报告"→ 提示用户重新上传
- 用户重复上传同一图片 → 正常处理，无需去重
- 用户在上传过程中关闭页面 → 无状态，不处理

# 7. 页面结构设计（所有页面+跳转关系+HTML原型）

## 页面清单（MVP仅1个页面）
当前MVP是一个单页应用（SPA），所有交互都在同一个HTML页面上完成。

### 单页面结构
```
┌──────────────────────────────────────┐
│         医疗报告助手（Header）         │  ← 蓝色背景，白色文字
├──────────────────────────────────────┤
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 欢迎使用医疗报告助手！请上传您的  │  │
│  │ 医疗检测报告图片以获取分析和健康  │  │
│  │ 建议。                          │  │  ← 欢迎语（白色卡片）
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ [用户上传的图片缩略图]          │  │  ← 用户消息（右对齐，蓝色气泡）
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ ● ● ●  5秒                    │  │  ← 加载动画（左对齐，白色卡片+三点跳动+计时器）
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 检测结果分析                    │  │
│  │ ─────────────────             │  │
│  │ 异常指标：                      │  │
│  │ • 血糖: 6.8mmol/L ↑偏高       │  │  ← 服务器响应（左对齐，白色卡片）
│  │ • 总胆固醇: 5.6mmol/L ↑偏高   │  │    Markdown渲染
│  └────────────────────────────────┘  │
│                                      │
│  ┌────────────────────────────────┐  │
│  │ 健康建议                        │  │
│  │ ─────────────────             │  │
│  │ 饮食建议：减少高糖食物摄入...   │  │  ← 服务器响应（左对齐，白色卡片）
│  │ 运动建议：每周150分钟有氧...   │  │    Markdown渲染
│  └────────────────────────────────┘  │
│                                      │
├──────────────────────────────────────┤
│ ┌──────────────┐  ┌──────────────┐  │
│ │  + 选择图片   │  │    发送      │  │  ← 底部输入区域
│ └──────────────┘  └──────────────┘  │
│   [图片预览区域 - 选中后显示]        │
└──────────────────────────────────────┘
```

### 页面跳转关系
MVP无页面跳转。后续P1版本会增加：
```
首页 (聊天界面) → 登录/注册页面 (第一个未登录状态跳转)
首页 → 历史记录页面 (点击"历史"按钮)
历史记录页面 → 详情页面 (点击某条记录)
```

# 8. 数据结构设计

## 当前MVP（无数据库）
所有数据在内存/请求周期内完成，不持久化。

## P1版本数据库设计（SQLite/PostgreSQL）

### 用户表 (users)
```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 问诊记录表 (consultations)
```sql
CREATE TABLE consultations (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    image_hash VARCHAR(64),              -- 图片SHA256哈希（用于去重）
    original_filename VARCHAR(255),
    image_size_bytes INTEGER,
    image_width INTEGER,
    image_height INTEGER,
    vl_model_name VARCHAR(100),           -- VL模型版本
    llm_model_name VARCHAR(100),          -- LLM模型版本
    vl_prompt TEXT,                       -- VL提示词
    llm_prompt TEXT,                      -- LLM提示词
    analysis_result TEXT,                 -- VL分析结果
    health_recommendations TEXT,           -- LLM建议
    latency_ms INTEGER,                   -- 处理延迟（毫秒）
    user_rating SMALLINT,                 -- 用户评分 1-5
    feedback TEXT,                        -- 用户反馈
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 模型版本表 (model_versions)
```sql
CREATE TABLE model_versions (
    id SERIAL PRIMARY KEY,
    model_type VARCHAR(20) NOT NULL,       -- 'llm' 或 'vl'
    model_name VARCHAR(100) NOT NULL,
    base_model VARCHAR(100),               -- 基座模型名称
    lora_adapter_path VARCHAR(500),        -- LoRA adapter路径
    training_dataset VARCHAR(500),         -- 训练数据集
    training_date TIMESTAMP,
    eval_bleu_score FLOAT,
    eval_rouge_score FLOAT,
    is_active BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 聊天消息表
```sql
CREATE TABLE chat_messages (
    id SERIAL PRIMARY KEY,
    consultation_id INTEGER REFERENCES consultations(id),
    role VARCHAR(20) NOT NULL,             -- 'user' 或 'assistant'
    content TEXT NOT NULL,
    message_type VARCHAR(20),              -- 'text' 或 'image'
    image_data TEXT,                       -- Base64编码图片（可选）
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

# 9. 技术依赖说明

## 核心依赖
| 依赖 | 版本 | 用途 | 是否必须 |
|------|------|------|----------|
| Python | >=3.12 | 运行时 | 是 |
| PyTorch | >=2.1 | 深度学习框架 | 是 |
| vLLM | ==0.11.2 | 模型推理引擎 | 是 |
| FastAPI | >=0.100 | Web框架 | 是 |
| uvicorn | >=0.20 | ASGI服务器 | 是 |
| httpx | >=0.24 | HTTP客户端 | 是 |
| Pillow | >=10.0 | 图片处理 | 是 |
| unsloth | latest | LoRA微调框架 | 训练阶段 |
| trl | latest | 监督微调 | 训练阶段 |
| transformers | latest | HuggingFace生态 | 训练阶段 |
| datasets | latest | 数据集加载 | 训练阶段 |
| evalscope | latest | 模型评测 | 评测阶段 |

## 硬件依赖
| 硬件 | 规格 | 说明 |
|------|------|------|
| GPU | NVIDIA A100 80G (推荐) | 双模型同时加载~60GB显存 |
| GPU | RTX 4090 24G (最低) | 只能一次加载一个模型，需切换 |
| RAM | >=32GB | 模型加载和数据预处理 |
| 磁盘 | >=200GB | 基座模型（~30GB/个）+ 训练数据 |
| 网络 | 千兆网 | 模型下载和API调用 |

## 模型文件依赖
| 模型 | 大小 | 来源 |
|------|------|------|
| Qwen2.5-14B | ~30GB | HuggingFace: Qwen/Qwen2.5-14B |
| Qwen2.5-VL-7B | ~18GB | HuggingFace: Qwen/Qwen2.5-VL-7B-Instruct |
| LLM LoRA adapter | ~100MB | 训练产出 |
| VL finetuned model | ~18GB | 训练产出（VL模型合并后） |

# 10. 风险分析

## 技术风险
| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| LoRA微调效果不达预期 | 中 | 高 | 设置自动评测基准，BLEU/ROUGE不达标则回滚至参数调整；准备人工评估集 |
| VL模型识别准确率低 | 中 | 高 | 提供多张报告训练样本；使用data augmentation扩充；人工标注更多高质量图片 |
| 双模型显存不足 | 中 | 高 | 启用4bit量化；分批加载模型；设置gpu_memory_utilization；升级硬件 |
| 模型推理延迟过长 | 高 | 中 | 前端加载动画+计时器给用户反馈；优化vLLM参数；考虑streaming输出 |
| 图片格式兼容性 | 低 | 中 | Pillow支持多种格式；转换失败时返回明确错误提示 |
| 中文报告OCR识别困难 | 中-高 | 高 | 微调VL模型时加入多样化的报告图片；对于手写体报告提示用户重新上传清晰照片 |

## 成本风险
| 风险 | 说明 | 应对 |
|------|------|------|
| GPU租赁成本 | A100 80G云服务器~$2-3/小时，月费可观 | 按需使用，训练完成后释放；推理用低配实例 |
| 数据标注成本 | 医学报告图片标注需要专业人士 | 初期用公开数据集+自标注；后期考虑半自动化标注 |
| 模型存储 | 基座模型+adapter约50GB存储 | 使用对象存储（S3/OSS）托管模型文件 |

## 性能风险
| 指标 | 目标 | 风险 |
|------|------|------|
| 端到端延迟 | <30秒 | 双模型串行调用，加网络传输，可能>30秒 |
| 并发能力 | 5 QPS | vLLM单卡并发有限，LoRA加载有额外开销 |
| 图片大小 | <10MB | 手机拍照可能>10MB，需要主动压缩 |

## AI幻觉风险
| 风险场景 | 影响 | 缓解措施 |
|----------|------|----------|
| 错误解读指标 | 用户可能做错误健康决策 | 前端加免责声明："本建议仅供参考，不构成医疗诊断" |
| 过度自信输出 | 用户忽略真实就医需求 | 在建议中加"建议咨询医生"的强调 |
| 漏检重要异常 | 用户误以为"一切正常" | VL模型明确要求输出"未发现异常指标"而非沉默；添加confidence评分 |
| 产生不存在的数据 | 模型编造指标数值 | 限制输出范围，要求VL只提取报告中明确标注的内容 |

## 数据安全风险
| 风险 | 说明 | 应对 |
|------|------|------|
| 患者隐私泄露 | 医学报告含姓名/ID等个人信息 | 所有处理在本地完成；不上传第三方API；传输使用HTTPS |
| 数据被二次训练 | 用户上传的图片可能被用于模型训练 | 隐私政策明确告知数据用途；提供删除选项 |
| 合规性 | 未经NMPA认证的AI医疗建议有法律风险 | 产品定位为"健康建议"而非"诊断"，区分医疗与健康管理 |

# 11. 商业化设计

## 收费模式
| 模式 | 说明 | 定价参考 |
|------|------|----------|
| 免费版（MVP） | 每天3次免费分析 | 拉新获客 |
| 个人专业版 | 无限次分析 + 历史记录 + 多报告趋势 | ￥29/月 或 ￥199/年 |
| 家庭版 | 5个家庭成员账号 + 共享报告管理 | ￥69/月 或 ￥499/年 |
| 企业版 | 私有化部署 + 定制微调 + 管理后台 | 按需报价（￥5-20万/年） |

## 付费点
| 付费点 | 归属版本 | 说明 |
|--------|----------|------|
| 无限次报告分析 | 专业版/家庭版 | 免费版每日限额 |
| 历史记录云存储 | 专业版/家庭版 | 免费版仅本地存储 |
| 多报告趋势图表 | 专业版/家庭版 | 跨时间指标变化可视化 |
| PDF报告导出 | 专业版/家庭版 | 专业版去水印 |
| 多轮对话追问 | 专业版/家庭版 | 免费版仅单次分析 |
| 用药提醒 | 家庭版 | 基于诊断的智能提醒 |
| 私有化部署 | 企业版 | On-premise，数据不出内网 |
| 定制LoRA微调 | 企业版 | 基于企业自有数据的专属模型 |
| 管理后台/看板 | 企业版 | 团队健康数据仪表盘 |

## 转化路径
```
免费用户 (每天3次)
    │
    ├── 用完免费额度 → 弹出升级提示 → 转化
    │
    ├── 看到分析结果的价值 → 点击"保存到云端" → 需要注册 → 转化
    │
    ├── 想要趋势分析 → 点击"报告趋势" → 需要专业版 → 转化
    │
    └── 企业用户 → 在页面底部看到"企业版"链接 → 留资咨询 → 转化
```

## 免费版运营策略
- 新用户注册即送7天专业版体验
- 分享分析结果到朋友圈再送3次免费分析
- 每日签到送1次额外分析

# 12. 指标体系（KPI）

## 产品指标
| 指标 | 目标值 | 说明 |
|------|--------|------|
| DAU | MVP期200+ | 日活跃用户数 |
| 日均分析次数 | 500+ | 核心使用行为 |
| 上传→完成率 | >85% | 用户上传后完成全流程的比例 |
| 平均处理延迟 | <20秒 | 端到端VL+LLM处理时间 |
| 用户留存(D7) | >30% | 7天后回访用户比例 |
| 用户留存(D30) | >15% | 30天后回访用户比例 |
| 用户评分 | >4.0/5.0 | 每次分析后用户可评分 |
| NPS | >40 | 净推荐值 |

## 模型指标
| 指标 | 目标值 | 说明 |
|------|--------|------|
| BLEU-4 | >0.35 | 微调后模型 vs 标准答案的词重叠度 |
| ROUGE-L | >0.50 | 微调后模型 vs 标准答案的召回率 |
| VL异常识别准确率 | >90% | 人工标注测试集的准确率 |
| 安全输出率 | 100% | 输出不含危险医疗建议 |
| 幻觉率 | <5% | 输出包含虚构指标/数据的比例 |

## 商业指标
| 指标 | 目标值 | 说明 |
|------|--------|------|
| 免费→付费转化率 | >5% | 注册用户付费比例 |
| 月均ARPU | >￥15 | 每付费用户月均收入 |
| 企业版销售线索 | 10+/月 | 企业咨询量 |
|| 客户生命周期价值(LTV) | >￥300 | 用户平均付费总额 |
|

---

# 📊 评估数据来源与 PM 自测用例

## 评估数据来源

本项目评估数据来自三部分：

| 数据来源 | 内容 | 数量 |
|---------|------|------|
| 训练集 | 医疗 QA 对（症状描述 + 专业建议），格式为 {"INPUT": "...", "OUTPUT": "..."} | 1,000 条 |
| 测试集 | 与训练集无重叠，用于最终评估（BLEU / ROUGE 对比） | 256 条 |
| Ground Truth | 测试集的 OUTPUT 字段即为标准答案，由医疗背景人员编写 | 256 条 |

**评测方式**：使用 EvalScope 框架，在相同的 256 条测试集上分别评测**原始模型**和**LoRA 微调模型**，对比 BLEU / ROUGE 指标。

## PM 自测用例（10 条）

以下测试用例由 AI 产品经理编写，覆盖常见医疗咨询场景和边界情况。每条包含用户输入和期望回答的关键要素。

```python
test_cases = [
    # 场景1：常见指标异常
    {"input": "我最近体检发现血糖偏高，6.8mmol/L，应该怎么办？",
     "expected_elements": ["饮食控制", "运动", "复查"]},
    # 场景2：紧急情况判断
    {"input": "我肚子疼了一天了，还发烧38.5度，要不要去医院？",
     "expected_elements": ["立即就医", "不自行用药"]},
    # 场景3：症状咨询
    {"input": "最近总是头晕，特别是站起来的时候，是什么问题？",
     "expected_elements": ["低血压可能", "建议就医检查"]},
    # 场景4：指标解读
    {"input": "我尿酸480，需要注意什么？",
     "expected_elements": ["饮食控制", "嘌呤", "复查"]},
    # 场景5：特殊人群用药
    {"input": "孕妇可以吃感冒药吗？",
     "expected_elements": ["咨询医生", "不自行用药"]},
    # 场景6：生活方式咨询
    {"input": "我每天睡6小时但白天还是困，怎么回事？",
     "expected_elements": ["睡眠质量", "就医检查"]},
    # 场景7（边界）：打招呼
    {"input": "你好", "expected_elements": ["问好", "询问需求"]},
    # 场景8（边界）：情绪化
    {"input": "我百度查了一下说可能是癌症，我很害怕",
     "expected_elements": ["安抚情绪", "不自行诊断", "建议就医"]},
    # 场景9（边界）：英文输入
    {"input": "I have a headache for 3 days",
     "expected_elements": ["英语回答", "就医建议"]},
    # 场景10（边界）：多问题混杂
    {"input": "我最近血压高，150/95，同时偶尔心慌，有关系吗？",
     "expected_elements": ["血压分析", "心慌关联", "就医建议"]},
]
```

**通过标准**：10 条全部通过后进入正式评估。每条回答需包含所有 expected_elements。

---

## ROI 计算

### 成本

| 项目 | 金额 | 说明 |
|------|------|------|
| GPU 训练（1×A10, 4小时） | ¥60 ≈ $8 | AutoDL 租用 |
| 数据准备（3人天） | $600 | 收集+清洗+标注 |
| 开发调试（2周） | $4,000 | 工程师成本分摊 |
| **一次性总开发成本** | **$4,608** | |
| API 运营（日均500次，Qwen-Plus） | $17/月 | $204/年 |
| 工程师维护（0.5人） | $25,000/年 | |

### 收益

| 来源 | 金额 | 说明 |
|------|------|------|
| 付费用户（200人 × $120/年） | $24,000/年 | |
| 总运营成本（含开发分摊） | $1,837/年 | 开发$4,608÷3年+运营$204+维护$0 |

**ROI = ($24,000 - $1,837) / $1,837 = 1,207%**
**回本周期：约 1 个月**

---

# 13. 与研发协作说明

## 团队角色与职责
| 角色 | 人数 | 职责 |
|------|------|------|
| 算法工程师 | 1-2人 | 数据准备、LoRA微调、模型评测、vLLM部署 |
| 后端工程师 | 1人 | FastAPI服务开发、vLLM对接、API设计 |
| 前端工程师 | 1人 | HTML/JS/CSS开发、交互设计、组件封装 |
| 产品经理 | 1人 | 需求定义、用户研究、验收测试 |
| 医学顾问 | 兼职 | 数据集标注审核、模型输出质量评估 |

## 交付物清单
| 阶段 | 交付物 | 验收标准 |
|------|--------|----------|
| 数据准备 | med-dataset-train/valid/test.json | 格式正确，数据无重复 |
| LLM微调 | qwen25-14b-fine-tuned-lora/ | BLEU>0.35，ROUGE>0.50 |
| VL微调 | qwen25-vl-7b-finetuned/ | 异常识别准确率>90% |
| 后端服务 | fastapi_v4_ui.py | POST /image 端到端测试通过 |
| 前端UI | index.html + style.css + script.js | 上传→加载→显示全流程通过 |
| 部署文档 | README + 启动脚本 | 新人按文档15分钟启动 |

## 沟通协作机制
| 仪式 | 频率 | 参与人 |
|------|------|--------|
| 每日站会 | 每日15min | 全部 |
| 模型效果Demo | 每周2次 | 算法+产品+医学顾问 |
| 前端联调 | 按需 | 前端+后端 |
| Sprint评审 | 每2周 | 全部 |
| 用例评审 | 模型调优前 | 算法+医学顾问 |

## 开发验收流水线
```
PM提需求 → 研发评估 → 开发 → 自测 → Code Review → 联调 → PM验收 → 上线
```
- 每个PR至少1人Review方可合并
- 模型效果变更需产品+医学顾问签字
- 前端UI改动需提交Figma原型
- API改动需更新API文档
